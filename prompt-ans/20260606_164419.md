这次不是前端 JSON 真坏了，而是 GUI 桥接进程压根没有稳定输出 JSON，所以 Rust 端在 `inspect_workflow` 里读到空 stdout，才报 `EOF while parsing a value at line 1 column 0`。

改了什么

- 重写了 [app/gui_cli.py](/D:/python/AI-Scientist/app/gui_cli.py)，补上 `if __name__ == "__main__": raise SystemExit(main())`，让 `python -m app.gui_cli ...` 真正执行。
- 重写了 [app/__init__.py](/D:/python/AI-Scientist/app/__init__.py)，把 `app.main` 改成惰性导入，避免包导入阶段就因为重量级依赖把 GUI 桥接提前打断。
- 调整了 [ui/src-tauri/src/lib.rs](/D:/python/AI-Scientist/ui/src-tauri/src/lib.rs)：
  - GUI 桥接报错改成可读中文；
  - 如果 stdout 为空，会明确提示“没有输出 JSON”；
  - Python 解释器选择逻辑改成优先用 `AI_SCIENTIST_PYTHON`、当前虚拟环境、`CONDA_PREFIX`、项目 `.venv`，最后再尝试用户目录下的 `C:\Users\GGDog\.conda\envs\AI-Scientist\python.exe`，不再盲目落到系统 `python`。

有没有验证

- `python -m py_compile` 已通过 `app/__init__.py`、`app/gui_cli.py`、`app/gui_api.py`。
- `python -m app.gui_cli --help` 已通过。
- 使用 [C:\Users\GGDog\.conda\envs\AI-Scientist\python.exe](C:\Users\GGDog\.conda\envs\AI-Scientist\python.exe) 实测执行：
  - `python -m app.gui_cli --project-root D:\python\AI-Scientist state --thread-id test`
  - 已经能稳定输出合法 JSON，不再是空输出或 EOF。
- `npm run build` 已通过。

还剩什么风险

- 你必须重启当前正在运行的 `Tauri` 窗口或 `npm run tauri dev` 进程，因为 Rust 侧解释器选择逻辑和报错处理已经改了，旧进程不会热更新这部分。
- 如果重启后还有报错，新的报错内容应该会更具体，届时继续发我界面底部文本即可，不会再是之前那种只有 `EOF` 的空提示。
