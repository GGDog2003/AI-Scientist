问题根因已经确认：你当前环境里没有任何模型密钥，`OPENAI_API_KEY=False`、`ANTHROPIC_API_KEY=False`、`GOOGLE_API_KEY=False`，所以之前程序才会“秒出”。那不是 Claude Code 那种真实调用大模型后的行为，而是项目原先的本地 stub 假流程。

我已经把这个行为改掉了。现在 [app/config.py](/D:/python/AI-Scientist/app/config.py) 不会再在“没配密钥”时偷偷回退到 stub，而是直接报错；[app/main.py](/D:/python/AI-Scientist/app/main.py) 也会在启动时打印当前运行模式和识别到的 PDF 数量。也就是说，你现在再执行 `python main.py start ...`，如果没配密钥，会明确报错提醒你先配置；如果配了密钥，就会进入真实大模型调用流程，而不是秒出。

这次我做了两类验证。第一，`python -m compileall main.py app tests` 语法通过。第二，我直接检查了当前环境变量，确认你现在确实没有配置任何大模型 API Key。

剩余风险有两个。第一，这个项目现在是“按节点调用大模型”，不是像 Claude Code 那样的逐 token 交互式流式终端体验，所以即使接上真实模型，也更像阶段式工作流执行器，而不是全程对话式 coding agent。第二，当前虽然已经能阻止静默 stub，但你要先自己配置至少一个模型密钥，程序才能真的跑到外部大模型。
