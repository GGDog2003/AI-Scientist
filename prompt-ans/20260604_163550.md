改了两块。第一，我新增了仓库根目录入口 [main.py](/D:/python/AI-Scientist/main.py)，现在默认可以直接用 `python main.py start ...`、`python main.py resume ...`、`python main.py state ...`。第二，我重写了 [app/main.py](/D:/python/AI-Scientist/app/main.py) 的启动参数，新增 `--paper-dir`，程序会自动扫描目录下全部 `.pdf` 并按文件名顺序逐篇加入工作流；`--paper-path` 只保留为补充参数。文档也同步更新到了 [README.md](/D:/python/AI-Scientist/README.md) 和 [实现方式/20260604_110941_hello-agents科研agent实现细节.md](/D:/python/AI-Scientist/实现方式/20260604_110941_hello-agents科研agent实现细节.md)。

有做验证。我跑了 `python -m compileall main.py app tests`，语法通过；另外尝试了 `python main.py --help` 和 `python main.py start --help`，但当前环境缺少 `langgraph` 依赖，所以运行时被 `ModuleNotFoundError: No module named 'langgraph'` 阻塞，这属于环境依赖问题，不是这次入口改动本身的语法错误。

剩余风险主要有两个。第一，当前只扫描 `--paper-dir` 目录下一层的 `*.pdf`，如果你还想递归子目录，需要再补成 `rglob`。第二，在依赖没装齐前，`python main.py ...` 只能完成语法级验证，不能做真实启动验证。
