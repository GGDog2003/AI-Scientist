原因就是创新点评审阶段的路由语义写错了。你这次 `advisor` 给的是 `major_revision`，但代码原先只把 `revise` 视为“回到研究生继续改”，其余所有非 `approved` 结果都会落到 `finalize_node`。所以流程不是异常崩掉，而是被系统当成“正常结束”，于是进程 `exit code 0`。

我已经修改了 [app/workflows/state_machine.py](/D:/python/AI-Scientist/app/workflows/state_machine.py) 和 [src/ai_scientist/workflow.py](/D:/python/AI-Scientist/src/ai_scientist/workflow.py)。现在在**创新点评审阶段**，`major_revision` 会和 `revise` 一样进入 `innovation_revision`，并由 `innovation_router` 跳回 `student_innovation_react_node`，不会再因为导师要求大修就直接结束流程。

有没有验证：做了，运行了 `python -m py_compile app\workflows\state_machine.py src\ai_scientist\workflow.py`，语法通过。

还剩什么风险：这次修的是“创新点评审里的 `major_revision` 被错误当成结束态”这个逻辑问题，所以你这类 `process exit with code 0` 应该会消失；但如果后面模型再返回别的非预期决策值，仍可能暴露新的状态机分支问题。
