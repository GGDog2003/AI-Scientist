# 本次答复

已按实现细节文档里的“2. 推荐目录结构”把项目正式整理到 `app/` 分层结构下，并把运行入口切到 `app.main`。

## 这次完成的结构重构

- 新增并补齐 `app/agents`
- 新增并补齐 `app/workflows`
- 新增并补齐 `app/tools`
- 新增并补齐 `app/memory`
- 新增并补齐 `app/schemas`
- 新增并补齐 `app/prompts`
- 新增 `app/main.py` 与 `app/__main__.py`
- 新增 `workspace/*` 各目录占位文件
- `pyproject.toml` 的打包入口改为只打包 `app`

## 关键调整

- `app` 不再只是转发壳，配置、Schema、工具、消息总线、状态机和 CLI 主入口都已切到 `app/`
- 现在正式入口是 `app.main:main`
- `README.md` 也已经改成按 `app/` 目录说明

## 已做验证

- `python -m compileall app tests`
- `app` 目录源码中已无对旧 `ai_scientist` 实现层的源码依赖

## 当前风险

- `src/ai_scientist` 目录还保留在仓库里，但已不再作为打包入口
- 如果你下一步希望仓库彻底只剩 `app/` 结构，还可以继续删除旧兼容层和 `__pycache__`
