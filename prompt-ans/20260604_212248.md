这次不是模型 structured output 又坏了，而是工作流路由表本身有 bug。上次我把 `innovation_router` / `paper_router` 改成了在拒绝或超过返修上限时返回 `finalize_node`，但 [app/workflows/state_machine.py](/D:/python/AI-Scientist/app/workflows/state_machine.py) 的 `add_conditional_edges(...)` 映射表没有把 `finalize_node` 注册进去，所以 LangGraph 在执行到 `advisor_innovation_review_react_node` 后分支跳转时直接抛了 `KeyError: 'finalize_node'`。

我已经修复了 [app/workflows/state_machine.py](/D:/python/AI-Scientist/app/workflows/state_machine.py) 和 [src/ai_scientist/workflow.py](/D:/python/AI-Scientist/src/ai_scientist/workflow.py)：一是把返回 `finalize_node` 的条件边映射补齐；二是把 `src` 那套旧工作流也同步成和 `app` 一样的有限轮次策略，避免两套入口逻辑不一致。

有没有验证：做了，运行了 `python -m py_compile app\workflows\state_machine.py src\ai_scientist\workflow.py`，语法通过。

还剩什么风险：这次修复的是分支路由崩溃，所以 `KeyError: 'finalize_node'` 应该不会再出现；但后续如果模型输出继续极不稳定，仍可能在别的节点触发新的业务逻辑问题。下一步最直接的验证方式就是重新跑你刚才那条 `python main.py start ...`，看它是否能顺利从创新点评审继续走到结束或下一挂起点。
