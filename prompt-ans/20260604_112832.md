# 本次答复

已把“全局 `Plan-and-Solve`，单个 Agent 内部 `ReAct`”正式补充进主方案文档：

- [20260604_110941_hello-agents科研agent实现细节.md](D:\python\AI-Scientist\实现方式\20260604_110941_hello-agents科研agent实现细节.md)

这次补充的重点有：

- 新增 `2.2 推荐范式：Plan-and-Solve 外壳 + ReAct 内核`
- 把 `StudentAgent`、`AdvisorAgent`、`ReviewerAgent` 都明确成局部 `ReAct Agent`
- 把状态机层明确成全局 `Plan-and-Solve`
- 增加了创新点、论文审核、盲审返修的回环说明
- 增加了 `LangGraph` 下的角色化节点命名建议
- 增加了一个可直接照着实现的 Router 伪代码

## 改了什么

- 更新主方案文档，补充混合范式与回环设计
- 新增本轮用户请求归档文件
- 新增本轮答复归档文件

## 有没有验证

- 已完成文档修改
- 本轮未做代码运行验证，因为只涉及方案文档更新

## 还剩什么风险

- 当前仍然是方案层，没有把这些节点真正实现成 `LangGraph` 代码
- 如果后面要开工，下一步最合适的是直接生成项目骨架和 `StateGraph` 初版
