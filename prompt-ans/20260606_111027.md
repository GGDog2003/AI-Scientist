**改了什么**
- 在 [app/config.py](/D:/python/AI-Scientist/app/config.py) 和 [src/ai_scientist/config.py](/D:/python/AI-Scientist/src/ai_scientist/config.py) 增加了 `llm_max_retries`、`llm_retry_backoff_seconds`，分别对应环境变量 `AI_SCIENTIST_LLM_MAX_RETRIES` 和 `AI_SCIENTIST_LLM_RETRY_BACKOFF_SECONDS`，默认都是重试 3 次、基础退避 3 秒。
- 在 [app/agents/base_agent.py](/D:/python/AI-Scientist/app/agents/base_agent.py) 和 [src/ai_scientist/agents.py](/D:/python/AI-Scientist/src/ai_scientist/agents.py) 的真实 LLM 调用边界加了统一重试封装。现在 `model.invoke(...)` 和 `agent.invoke(...)` 遇到 `524`、`timeout`、`429`、`temporarily unavailable` 这类可恢复错误时，会先重建模型连接再重试；`agent` 路径每次重试也会重建一次，避免复用坏连接。

**有没有验证**
- 已做编译级验证：`py_compile` 通过了 [app/agents/base_agent.py](/D:/python/AI-Scientist/app/agents/base_agent.py)、[src/ai_scientist/agents.py](/D:/python/AI-Scientist/src/ai_scientist/agents.py)、[app/config.py](/D:/python/AI-Scientist/app/config.py)、[src/ai_scientist/config.py](/D:/python/AI-Scientist/src/ai_scientist/config.py)。

**还剩什么风险**
- 目前只验证了语法和编译，没有做真实联网压测，所以还没现场确认一次 `524` 后的自动恢复链路。
- 现在是“自动重连并重试”，但还没有把每次重试原因和次数写进日志；如果你后面要排查上游稳定性，建议再补一层日志。
